import React from 'react'

const index = () => {
  return (
    <nav>
        <span>LOGO</span>

        <div>
        <button>Login</button>
        <button>Sign Up</button>
        </div>
    </nav>
  )
}

export default index